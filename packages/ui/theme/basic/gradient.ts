import COLOR from './color'

const GRADIENT = {
  RED: `linear-gradient(90deg, ${COLOR.RED} 0%, ${COLOR.SALMON} 100%)`,
  RED_VERTICAL: `linear-gradient(180deg, ${COLOR.RED} 0%, ${COLOR.SALMON} 100%)`
}

export default GRADIENT
